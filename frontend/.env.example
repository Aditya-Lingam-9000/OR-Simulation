# OR-Symphony Frontend Environment Variables
#
# Copy this to .env.local and set your values.
# Vite automatically loads .env.local during development.

# Backend URL — set this when the backend runs on Kaggle (via ngrok)
# Leave empty/commented out when running locally with Vite proxy.
#
# Example: VITE_BACKEND_URL=https://abc123.ngrok-free.app
VITE_BACKEND_URL=
